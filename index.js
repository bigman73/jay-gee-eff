const { JGFContainer } = require('./jgfContainer');

const printMsg = () => {
    console.log('This is a message from the jay-gee-eff package');
};

module.exports = {
    JGFContainer,

    printMsg
};